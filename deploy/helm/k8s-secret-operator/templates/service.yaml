apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8s-secret-operator.fullname" . }}-metrics
  labels:
    {{- include "k8s-secret-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "k8s-secret-operator.selectorLabels" . | nindent 4 }}
